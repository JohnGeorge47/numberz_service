{
  "name": "reminder_service",
  "version": "1.0.0",
  "description": "A reminder service which is a small part of the numberz design",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "export NODE_ENV=local && nodemon --exec babel-node src/server.js",
    "build": "babel src --out-dir dist",
    "serve": "export NODE_ENV=qa && node dist/server.js"
  },
  "author": "JohnGeorge47",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mysql2": "^2.1.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.4",
    "@babel/core": "^7.10.4",
    "@babel/node": "^7.10.4",
    "@babel/preset-env": "^7.10.4",
    "nodemon": "^2.0.4"
  }
}
